<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">
  <uses-permission android:name="android.permission.CAMERA" />
  <uses-permission android:name="android.permission.RECORD_AUDIO" />
  <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />    
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Foreground service: keeps the JS thread alive for background BLE scan/advertise -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Android 14+: type declaration required for BLE-based foreground services -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />

    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        <!-- Deep link: markwise://attend?session=<sessionId> -->
        <intent-filter>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="markwise" />
        </intent-filter>
      </activity>

      <!--
        FileProvider — react-native-share v12 ships its own provider in its
        library manifest (cl.json.RNShareFileProvider, authority:
        "${applicationId}.rnshare.fileprovider") but its bundled
        share_download_paths.xml only covers cache-path and
        external Download/. We override it here to also cover the app's
        internal files directory (RNFS.DocumentDirectoryPath) where
        react-native-html-to-pdf writes PDFs.
        The tools:replace attribute merges this declaration over the
        library's own provider entry during the manifest merge.
      -->
      <!-- react-native-background-actions foreground service (API 34+ requires foregroundServiceType) -->
      <service
        android:name="com.asterinet.react.bgactions.RNBackgroundActionsTask"
        android:exported="false"
        android:foregroundServiceType="connectedDevice"
        tools:replace="android:exported,android:foregroundServiceType" />

      <provider
        android:name="cl.json.RNShareFileProvider"
        android:authorities="${applicationId}.rnshare.fileprovider"
        android:exported="false"
        android:grantUriPermissions="true"
        tools:replace="android:authorities,android:exported,android:grantUriPermissions">
        <!-- tools:node="replace" replaces the library's meta-data element wholesale
             rather than trying to merge the android:resource attribute, which
             ensures our file_paths.xml (covering all directories) wins over the
             library's share_download_paths.xml (which only covers cache + Download). -->
        <meta-data
          android:name="android.support.FILE_PROVIDER_PATHS"
          android:resource="@xml/file_paths"
          tools:node="replace" />
      </provider>
    </application>
</manifest>
