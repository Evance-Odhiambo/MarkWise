# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=512m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8

# Run subprojects in parallel — safe for decoupled Android modules.
org.gradle.parallel=true

# Keep the Gradle daemon alive between builds to avoid JVM cold-start cost.
org.gradle.daemon=true

# Cache task outputs so unchanged tasks are skipped on subsequent builds.
org.gradle.caching=true

# Only configure projects that are actually needed for the requested tasks.
org.gradle.configureondemand=true

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true

# Use this property to specify which architecture you want to build.
# You can also override it from the CLI using
# ./gradlew <task> -PreactNativeArchitectures=x86_64
reactNativeArchitectures=armeabi-v7a,arm64-v8a

# Use this property to enable support to the new architecture.
# This will allow you to use TurboModules and the Fabric render in
# your application. You should enable this flag either if you want
# to write custom TurboModules/Fabric components OR use libraries that
# are providing them.
newArchEnabled=true

# Use this property to enable or disable the Hermes JS engine.
# If set to false, you will be using JSC instead.
#
# IMPORTANT — release builds require hermesc to exactly match hermes-android version:
#   RN 0.84 ships hermes-android@250829098.0.9 (Hermes V1).
#   The hermesc must be from hermes-compiler@250829098.0.9 (same version).
#   If a mismatched hermesc is used (e.g. the old @0.17.0 pin), the APK crashes.
#   Debug builds are unaffected (Metro serves JS; hermesc is never invoked).
#   Release APKs are safest when built via GitHub Actions (.github/workflows/build-release-apk.yml).
hermesEnabled=true

# Use this property to enable edge-to-edge display support.
# This allows your app to draw behind system bars for an immersive UI.
# Note: Only works with ReactActivity and should not be used with custom Activity.
edgeToEdgeEnabled=false

# ---------------------------------------------------------------------------
# Release signing — set via environment variables on CI, or locally for
# debug/release builds. Keep the keystore file out of version control.
# ---------------------------------------------------------------------------
MYAPP_UPLOAD_STORE_FILE=markwise-release.keystore
MYAPP_UPLOAD_KEY_ALIAS=markwise
MYAPP_UPLOAD_STORE_PASSWORD=markwise123
MYAPP_UPLOAD_KEY_PASSWORD=markwise123
